# 用法
```bash
./server -p 127.0.0.1:8080 -f 2000 -t 100000.0  #地址：端口，-f为每个帧的大小，单位是B,统一时间基准
```
```bash
./client -p 127.0.0.1:8080 -f 300 -t 100000.0  #地址：端口，-f为请求帧的个数，总时间=30ms * 帧数，统一时间基准
```
保证两端的-t传入时间一致，最好用系统的当前时间